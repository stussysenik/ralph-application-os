# Ralph Patches

Tracked semantic patch documents live here.

- `examples/` contains runnable patch documents that demonstrate model correction flows.
- Patch documents are typed runtime inputs, not ad hoc notes.
- The normal flow is: diff a model, write or refine a patch, apply it, inspect proof, then decide whether to promote the result further.
