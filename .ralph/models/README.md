# Ralph Models

Tracked semantic models live here once drafts are promoted into durable repo assets.

- `generated/` is for system-generated models promoted from drafts.
- `examples/` is for runnable branch fixtures that exercise diff, patch, and merge behavior.
- Human-edited tracked models can coexist later as the product grows a richer model store.
